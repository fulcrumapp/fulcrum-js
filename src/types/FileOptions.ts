//TODO: this is from FormData.AppendOptions
export default interface FileOptions {
  header?: string | Headers;
  knownLength?: number;
  filename?: string;
  filepath?: string;
  contentType?: string;
}
